/// @description  instance_at_coordinates(x_pos, y_pos, instance)
function instance_at_coordinates(argument0, argument1, argument2) {
	var x_pos = argument0, y_pos = argument1, instance = argument2;

	return (instance.x == x_pos && instance.y == y_pos)



}
