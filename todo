
restructure inheritance and parent objects

fix rooms to add back key spots
fix key and lock generation

make game yyc compatible

fix frame skip on room transition - due to bug: can't :)
fix random crashes?? (maybe due to this bug: In Game: Some projects crash silently when changing rooms if garbage collector is enabled)
- (tried turning off garbage collector - we'll see what else this breaks)

Is this a thing: Building Projects: Xcode 12 breaks iOS/tvOS builds (There is also a guide for fixing 2.3.0 in the FAQs list below!]

2794973601 // keys already opened chest (not map?)
3367961002 with target_number_of_rooms = 1
2585055493 // wiggle under column?????0'

BUG FIXES
Make sure no rooms have doors on edges
Check that doors are created in both rooms when joined by a locked exit

Carrying two torches makes them always stay lit
Torches make extingush sound when lit by other torches
Torch should still burn down when not carried
Map colors don't blend with bg appropriately

Better keep track of what items have been spawned and when to spawn a red one
Refactor player with torches and lighthing holding things

Remove lit rooms coloring different on map


NEW FEATURES
Multi-tile worm that moves one space at a time and brings any associated worm bodies with him of varying length.
- Worm bodies can be solid? But not head?
- Must hit head with sword to kill it?
Revamp rosary item to respawn player at the start upon being killed (and drop any items they were carrying on the players corpse)
- leave behind a player corpse where they were killed
- add player corpe decorative tile as tile that very rarely (1/100?) spawns instead of regular corpses/bodies
- red rosary doesn't use itself up when being killed - if holding multiple rosaries, use up one and drop the other, and if holding red one use up none (like swords)
Add flecks/dots randomly generated on first visit to room to further decorate the space
Worm man runs straight and turns at walls
"Blind Monster" That moves toward whatever makes a sound
Stationary Monster that spits projectile?
Sometimes spawn a block on top of stairs if you didn't enter from stairs
Make keys and items not fight for space on top of stairs
